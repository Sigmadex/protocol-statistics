import { SwapModal } from "../SwapModal";
import { SelectToken } from "../SelectToken";

export const Swap = () => {
  return (
    <div className="row" style={{ marginBottom: 160 }}>
      {/* <SelectToken /> */}
      <SwapModal />
    </div>
  );
};
